using AventStack.ExtentReports;

namespace plataforma_automatizada.utils
{
    enum StatusTest
    {
        INFO = Status.Info,
        WARN = Status.Warning,
        SKIP = Status.Skip,
        PASS = Status.Pass,
        FAIL = Status.Fail
    }
}
